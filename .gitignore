
# Local working copy of the demo-authoring template. The tracked source of
# truth is crypto-lab/audits/_MASTER-TEMPLATE.md; a committed copy here would
# age silently and become a second, wrong standard.
CRYPTO-LAB-TEMPLATE*.md

# This demo's brief, likewise a local working input. No sibling lab in the
# fleet tracks one, and a file shape that exists in one repo out of ~180 is
# drift rather than provenance. The file stays on disk; it is just not part
# of what this repo ships.
brief.md

node_modules
dist
.DS_Store
*.local
playwright-report
test-results
